name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet restore EcoBilling.Control.slnx
      - run: dotnet test EcoBilling.Control.slnx --configuration Release
      - run: dotnet publish src/EcoBilling.Control.Api/EcoBilling.Control.Api.csproj --configuration Release --output artifacts/api
      - uses: actions/upload-artifact@v4
        with:
          name: ecobilling-control-${{ github.ref_name }}
          path: artifacts/
